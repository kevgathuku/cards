services:
  kadi:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - kadi-data:/app/data
    environment:
      - DATABASE_PATH=data/kadi.db
      - SESSION_SECRET=kadi-local-dev-session-key

volumes:
  kadi-data:
